# gitleaks configuration for SapoWhisper.
# Default rules plus path allowlist for local-only, git-ignored directories.

[extend]
useDefault = true

[allowlist]
paths = [
  '''^build/''',
  '''^DMG/''',
  '''^docs/''',
  '''^\.agents/''',
]
